# ansible-setup
Sets up the ansible user for my environment.

1. chmod +x setup_ansible_user.sh
2. sudo ./setup_ansible_user.sh

If you're running this for yourself for some reason:
- The implementation of the sudoers file is NOT ideal
- Change the public key for your ansible user
